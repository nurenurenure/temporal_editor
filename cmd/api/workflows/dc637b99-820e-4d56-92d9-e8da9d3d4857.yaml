document:
    dsl: 1.0.0
    taskQueue: zigflow
    workflowType: custom-wf-dc637b99-820e-4d56-92d9-e8da9d3d4857
    version: 0.0.1
    title: Loop and HTTP Call Test
    summary: Проверяем циклы и HTTP-вызовы (вместо Temporal Activity)
do:
    - forTaskArray:
        do:
            - send_email_notification:
                call: http
                with:
                    endpoint: ${ "https://api.example.com/notify/" + $data.item.email }
                    method: get
        for:
            each: item
            in: ${ $input.data }
